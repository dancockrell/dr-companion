{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core webview permissions. DR Companion's own commands (detection, bridge install, always-on-top) are app commands and are not granted here; no plugin is given shell, filesystem or network access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
